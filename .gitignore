
# Build directories
/build/
/build-*/
compile_commands.json

# Benchmark RUN artifacts (committed baselines live in bench/baselines/ and ARE
# tracked; these are fresh-run outputs the harness / CI writes to the repo root).
/latency.csv
/throughput.csv

# Nix
result
result-*

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Python bytecode (bench/plot.py)
__pycache__/
*.pyc
